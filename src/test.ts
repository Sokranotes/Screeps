export const test_work = function(creep: Creep){
    // if (creep.name == 'Repairer32103476')
    //     console.log(creep.move(TOP))
    // if (creep.name == 'Repairer32103192')
    //     console.log(creep.move(BOTTOM))

    // 哪怕相撞，也会走过去，但是都返回OK
}